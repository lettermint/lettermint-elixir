# Changelog

## Unreleased

- Add separate sending and team clients.
- Add all public API-key operations, generated models, and enum values.
- Add an immutable email builder, batch sending, and scheduled messages.
- Add webhook signature verification and API source contract tests.
